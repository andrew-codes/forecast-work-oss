import * as React from "react"
import useVss from "../vss/useVss"

const Forecasting = () => {
  const vss = useVss()
  console.log(vss)

  return <div>Hello world!</div>
}

export default Forecasting
